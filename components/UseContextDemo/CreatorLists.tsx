import CreatorCard from './CreatorCard'

export default function CreatorLists() {
  return (
    <div className="my-4">
      <CreatorCard isSelected={false} />
      <CreatorCard isSelected={false} />
      <CreatorCard isSelected={false} />
      <CreatorCard isSelected={false} />
      <CreatorCard isSelected={false} />
      <CreatorCard isSelected={false} />
    </div>
  )
}
